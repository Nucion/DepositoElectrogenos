<% if(stock){%>
<form action="/Sell/<%= productId%>/<%=purchase1.id%>" method="post">
    <div class="mb-3">
        <label for="Amount" class="form-label">Monto</label>
        <input type="number" class="form-control" id="Amount" name="monto">
    </div>
    <div class="mb-3">
        <label for="Client" class="form-label">Cliente</label>
        <input type="String" class="form-control" id="Client" name="cliente">
    </div>
    <button type="submit" class="btn btn-primary">Finalizado</button>
</form>

<%}else {%>
no hay stock xd
<%}%>
